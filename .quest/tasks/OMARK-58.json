{"id":"OMARK-58","aliases":[],"title":"promotion-guards prints 'genuine fast-forward' for a force-push rewind of main","status":"To Do","summary":"The main-is-fast-forward-of-dev job asserts only 'git merge-base --is-ancestor HEAD origin/dev' - that main's new HEAD is a commit dev holds. It never looks at where main WAS, so it cannot distinguish a forward promotion from a backwards rewind. A force-push moving main to an older commit that is still on dev satisfies the ancestor test and the job prints 'main's HEAD (...) is reachable from dev: genuine fast-forward.' The job is named 'main landed via fast-forward from dev' and measures only the 'from dev' half; the 'fast-forward' half is unmeasured and affirmatively claimed. github.event.before (and github.event.forced) are available on push events and unused.","description":"Raised as a hypothesis by the lore-cli session on 2026-09-15 via opum-agent: an assertion written inline in a workflow run: block can have no tests by construction, so whatever correctness it has is unmeasured. This job is exactly that shape - it is entirely inline in promotion-guards.yml, has no test surface, and only executes on a real push to main, so no run has ever exercised it except a real promotion.\n\nMeasured with a scratch bare repo (dev with c1..c4; main starting at c2), running the assertion verbatim on a fresh clone checked out at main's pushed SHA:\n  A. genuine promotion, push dev tip c4 over main c2   -> GREEN 'genuine fast-forward'   (correct)\n  B. force-push rewind, main c4 -> c1 (older, on dev)  -> GREEN 'genuine fast-forward'   (WRONG)\n  C. button-style merge commit pushed to main          -> RED                            (correct)\nScenario B erased two commits of shipped history from main and the guard affirmed it as genuine.\n\nThis is lore-cli's generalisation landing exactly: both halves of prove-it-rejects (C) and prove-it-accepts (A) pass, and the gate is still wrong, because the missing question is what its success message CLAIMS versus what it measured. A reader who sees 'genuine fast-forward' after an incident stops looking - which is worse than no message, since the rewind is precisely the state someone would be investigating.\n\nSeverity is bounded and should be stated honestly: force-push to main is already tier-3 forbidden by the fleet operating model and needs a human, so this is defense-in-depth failing open rather than a live exposure. That is also what the job is FOR - the comment in the file calls it 'defense in depth'. A defense-in-depth guard that affirms the thing it is defending against is the defect.","priority":"High","type":"bug","acceptanceCriteria":[{"index":0,"text":"The job asserts main moved FORWARD as well as from-dev: github.event.before is an ancestor of the new HEAD (skipping the check when before is the all-zero SHA, i.e. branch creation)","checked":false},{"index":1,"text":"github.event.forced is surfaced - a forced push to main is reported as such rather than absorbed into a green result, whatever the ancestor tests say","checked":false},{"index":2,"text":"The success message states both facts it measured (moved forward from <before>, and is a commit dev holds) rather than the single word genuine","checked":false},{"index":3,"text":"The assertion is extracted from the inline run: block into a script with a suite that drives it over a fixture repo, so it has tests at all - scenarios A, B and C above are the minimum cases","checked":false},{"index":4,"text":"Proven both directions with versions recorded: red on the rewind fixture with the rewind-specific message, green on a genuine fast-forward, and the other jobs green on both","checked":false}],"definitionOfDone":[],"plan":[],"implementationNotes":["Reproduction script kept at scratchpad ff.sh during the session; it is ~25 lines of bash over a scratch bare repo and is worth re-writing into scripts/test/ rather than recovered - the scratchpad is session-scoped.","Watch the zsh trap when re-deriving this by hand: \"$SHA:refs/heads/main\" is mangled by zsh, which reads :r as a variable modifier and silently pushes to a garbage refspec. Two runs of this proof produced confident-looking GREEN/RED output from pushes that had all failed. Use ${SHA}:refs/heads/main, or run it under bash.","Neither promotion-guards job is a required check, by design (the file explains why: requiring them would make the one correct promotion path unpushable). So this fix changes what a human reads after a promotion, not what blocks one. That is the point - the message is the entire product of this job.","Related in kind to OMARK-57, which is the same question (claim versus measurement) asked of the federated checkers' success messages.","REPLICATED INDEPENDENTLY in lore-cli, filed there as LCLI-514, 2026-09-15. Its ci.yml carries a main-is-fast-forward-of-dev job asserting merge-base --is-ancestor $GITHUB_SHA origin/dev with no read of event.before - same defect, different repo. lore-cli measured it in a scratch repo asserting the constructed state before the verdict: guard says GREEN on a rewind where a real fast-forward check (is-ancestor c4 c2) says RED. Its own comment calls it 'the guard that matters most for this repo specifically: main carries zero branch rules here', so the failing-open case is worse there than here. Two independent instances means the shape probably came from the shared opum-doc promote-dev-to-main runbook, which promotion-guards.yml cites - fixing it here without routing the shape back leaves the other copies wrong.","TEST-AUTHORING TRAP for the extracted assertion (same note on OMARK-57): scope assertions to the emitted verdict, not to the surrounding prose. This job's success string will grow to state two measured facts (moved forward from <before>, and is a commit dev holds); assert those two facts structurally rather than string-matching the sentence, or the test becomes a tripwire on wording.","REFERENCE IMPLEMENTATION, drafted 2026-09-15 at opum-agent's request (it ruled: one correct version written by the session that measured the defect, handed to the other seven owners as text, no session edits a sibling). Recorded here in full because the scratchpad it was built in is session-scoped. Proven 8/8 on a scratch bare repo: accepts a real promotion; rejects the rewind the old guard called genuine; names a forced push distinctly; rejects a commit dev never held; accepts branch creation without claiming forward movement; accepts an unchanged re-push; fails when the previous HEAD cannot be resolved; refuses to report at all when BEFORE_SHA is unset. Script body follows verbatim in the next note.","#!/usr/bin/env bash\n# Asserts that main's new HEAD (a) moved FORWARD from where main was, and\n# (b) is a commit dev already held. The original guard checked only (b) and\n# reported \"genuine fast-forward\" on a rewind - two commits could come off\n# main and it said green (opum-marketplace OMARK-58, lore-cli LCLI-514).\n#\n# Inputs, so this is runnable outside Actions and therefore testable at all:\n#   BEFORE_SHA  - github.event.before  (all-zero when the branch is created)\n#   FORCED      - github.event.forced  (\"true\"/\"false\")\nset -euo pipefail\n\nZERO=0000000000000000000000000000000000000000\nbefore=\"${BEFORE_SHA:?BEFORE_SHA not set - refusing to report on an unmeasured previous position}\"\nforced=\"${FORCED:-false}\"\n\ngit fetch --no-tags --quiet origin dev\nhead_sha=\"$(git rev-parse HEAD)\"\ndev_sha=\"$(git rev-parse origin/dev)\"\n\n# (b) came from dev. Unchanged from the original assertion.\nif ! git merge-base --is-ancestor \"$head_sha\" \"$dev_sha\"; then\n  echo \"::error::main's new HEAD ($head_sha) is not a commit dev ever held. This is not a promotion from dev - a merge button or a direct commit produces this. Do not push over it until the anomaly is understood; see opum-doc docs/runbooks/promote-dev-to-main.md, Rollback.\"\n  exit 1\nfi\n\n# A forced push to main is never needed for a fast-forward, so its presence is\n# the anomaly regardless of what the ancestor tests say. Checked separately so\n# the operator is told WHICH thing happened.\nif [ \"$forced\" = \"true\" ]; then\n  echo \"::error::main was FORCE-PUSHED ($before -> $head_sha). A fast-forward promotion never requires --force. Force-push to main needs your own user's direct authority; see opum-doc docs/runbooks/promote-dev-to-main.md.\"\n  exit 1\nfi\n\n# (a) moved forward. This is the half the original never measured.\nif [ \"$before\" = \"$ZERO\" ]; then\n  movement=\"created at $head_sha (main did not exist before this push, so there is no previous position to compare)\"\nelif ! git cat-file -e \"${before}^{commit}\" 2>/dev/null; then\n  echo \"::error::main's previous HEAD ($before) cannot be resolved in this clone, so forward movement CANNOT be proven. Treat as an anomaly, not as a pass - an object that is gone is usually one a rewrite orphaned.\"\n  exit 1\nelif [ \"$before\" = \"$head_sha\" ]; then\n  movement=\"unchanged at $head_sha\"\nelif git merge-base --is-ancestor \"$before\" \"$head_sha\"; then\n  movement=\"moved forward $before -> $head_sha\"\nelse\n  echo \"::error::main was REWOUND or diverged: its previous HEAD ($before) is not an ancestor of its new HEAD ($head_sha). Commits that were on main are no longer on main. This passes an is-ancestor-of-dev check, which is why that check alone was not enough. Do not push over it; see opum-doc docs/runbooks/promote-dev-to-main.md, Rollback.\"\n  exit 1\nfi\n\n# Says the two things it measured, not \"genuine fast-forward\" generally.\necho \"main $movement, and its HEAD is a commit dev holds (dev tip $dev_sha). Fast-forward from dev confirmed on both counts.\"","Workflow job calling the script above (the point of the extraction is that the assertion is now reachable by a test at all):\n  main-is-fast-forward-of-dev:\n    name: main landed via fast-forward from dev\n    if: github.event_name == 'push'\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          fetch-depth: 0\n      - name: main must have moved forward, and from dev\n        env:\n          BEFORE_SHA: ${{ github.event.before }}\n          FORCED: ${{ github.event.forced }}\n        run: ./scripts/ci/assert-main-fast-forward.sh\nfetch-depth: 0 is load-bearing twice over: the shallow default has neither the previous HEAD nor enough history for either is-ancestor test.","SEVERITY CORRECTION, 2026-09-16, and it raises this task's priority. This task and every message I sent about it said opum-marketplace was the defense-in-depth case because main is protected - 'a force-push rewind would still have to get past the required contexts'. THAT INFERENCE IS WRONG. quest-cli caught it on its own copy and it generalises; I then measured all five supposedly-protected repos and they are identical.\n\n  gh api repos/opum-ai/opum-marketplace/rulesets/22669384\n    name: require-ci-on-main | enforcement: active | bypass_actors: []\n    rules: required_status_checks ONLY\n    contexts: check - manifests, Tracker integrity, lore check\n    non_fast_forward rule: ABSENT     deletion rule: ABSENT\n\nSame for opum-agent, opum-doc, quest-cli and opum-cli-e2e - all five carry required_status_checks and nothing else, no non_fast_forward, no deletion, zero bypass actors.\n\nWhy protected=true is inert HERE SPECIFICALLY: required status checks gate a push on the PUSHED COMMIT'S OWN check history, not on which direction the branch moved. Check runs attach permanently to a SHA, so any commit previously promoted to main already carries three green contexts by construction - promotion is what put them there. Rewinding main onto such a commit is pushing a commit the ruleset is already satisfied by. Nothing named non_fast_forward exists to refuse it. Worse than quest-cli's framing for us: our three contexts also run on pushes to dev, so an arbitrary dev commit that was never on main likely carries them too, which widens the set of rewind targets the ruleset accepts.\n\nThe sharp version, quest-cli's: the ruleset backs up exactly the half of the guard that already works and provides nothing for the half that is broken. Assertion 1 (a foreign or merge-button commit) IS genuinely double-covered - such a commit has no prior green contexts. Assertion 2 (the rewind) has no second line at all.\n\nSo on this specific failure mode opum-marketplace is not in a different class from the rules=0 repos. Priority moved To Do/Medium -> To Do/High to match.\n\nEpistemic status, stated because this task is about claims outranging their measurements: this is strong inference from two API reads (rules/branches/main and the full ruleset), not an empirical test. I have not force-pushed main to confirm and will not - that is tier 3 and destructive. The absence of non_fast_forward in BOTH endpoints is the evidence; /rules/branches/main aggregates classic branch protection and rulesets into the same rule-type vocabulary, so its absence there covers the classic-protection case too.","PROCESS LESSON, same day, and it is the same defect class this task documents. I measured 'main rules=1 protected=true' (correct) and reported 'therefore a rewind still has to get past the required contexts' (wrong). The object I measured was WHETHER a rule exists; the object my claim was about was WHETHER THAT RULE REFUSES THIS PUSH. One gh api call apart, and I shipped the inference to four repositories as if it were the measurement. This is precisely the gate defect at human scale - the job measured containment and its name claimed fast-forwardness. When passing a severity signal onward, pass the rule TYPES, which are a fact, not the conclusion 'you are covered', which is an inference about a specific attack.","REFERENCE IMPLEMENTATION v2 supersedes the v1 recorded above. DO NOT IMPLEMENT v1. Three changes, all from peer review of the draft:\n\n(a) THIRD ASSERTION, from opum-fleet. v1 passed a stale-local-dev partial promotion - ODOC-193, 'git push origin dev:main' off a stale local dev instead of 'origin/dev:main'. That moves main forward to a commit dev genuinely holds, so v1's forward check AND containment check both went green while less was delivered than intended. VERIFIED against v1 before accepting the report: built main=c3 / dev=c4 on a scratch repo and v1 printed 'Fast-forward from dev confirmed on both counts', exit 0. v2 adds 'git rev-list --count HEAD..origin/dev'. It is a WARNING, NOT A RED, deliberately: dev legitimately advancing between the promotion push and the CI run produces a non-zero count with nothing wrong, and a red there would fire on correct behaviour - which is the exact defect that pushes a later author to weaken a check to get it green. This repo is directly exposed: our own CLAUDE.md cites ODOC-193 and requires the origin/dev:main form.\n\n(b) HEADER STATING WHAT THE JOB IS NOT, from opum-web. It runs on push, after the fact, and REPORTS - it does not block. Whether anything refuses a bad push is the branch rules, a separate mechanism. Without this a later session reads a green promotion job as a gate that stopped something. Also states it speaks only to GIT SHAPE: where a promotion includes a deploy or publish step, green here is not 'the promotion succeeded' (opum-web's OWEB-3: a commit produced no production deployment at all while git looked fine).\n\n(c) Containment error now names the unrecoverability - main stops being an ancestor of dev and can never fast-forward again - because opum-web reports the merge button is fully reachable there with allow_auto_merge=true and zero protection.\n\nProven 11/11 on a scratch bare repo: the original 8 cases plus exact-promotion-leaves-nothing-behind, partial-promotion-warns-but-stays-green, and partial-promotion-names-the-commit-left-behind. Script body verbatim in the next note.\n\nProvenance worth keeping: the guard had three independent gaps found by three different sessions against one copied shape that had never had a test anywhere - containment without forward movement (here), the job name claiming more than the assertion measured (lore-cli), and forward-and-contained but stale (opum-fleet).","#!/usr/bin/env bash\n# WHAT THIS IS, AND WHAT IT IS NOT. This runs on push to main, after the fact.\n# It REPORTS; it does not block. Whether anything actually refuses a bad push\n# depends on your repo's branch rules, which are a separate mechanism - check\n# `gh api repos/<owner>/<repo>/rules/branches/main` and do not assume a green\n# run here means something was stopped. It also speaks only about GIT SHAPE: if\n# your promotion includes a deploy, a publish or a propagation step, green here\n# is not \"the promotion succeeded\".\n#\n# Three properties, each asserted and reported separately so a failure names\n# which one broke. The original guard asserted only #2 and printed \"genuine\n# fast-forward\", claiming #1 and #2 together while measuring #2 alone\n# (opum-marketplace OMARK-58, lore-cli LCLI-514, quest-cli, opum-web OWEB-8).\n#\n#   1. main moved FORWARD          - github.event.before is an ancestor of HEAD\n#   2. main's HEAD CAME FROM dev   - HEAD is contained in origin/dev\n#   3. main IS dev, not part of it - nothing left behind (warning only, see below)\n#\n# Inputs, so this is runnable outside Actions and therefore testable at all:\n#   BEFORE_SHA - github.event.before (all-zero when the branch is created)\n#   FORCED     - github.event.forced (\"true\"/\"false\")\nset -euo pipefail\n\nZERO=0000000000000000000000000000000000000000\nbefore=\"${BEFORE_SHA:?BEFORE_SHA not set - refusing to report on an unmeasured previous position}\"\nforced=\"${FORCED:-false}\"\n\ngit fetch --no-tags --quiet origin dev\nhead_sha=\"$(git rev-parse HEAD)\"\ndev_sha=\"$(git rev-parse origin/dev)\"\n\n# ASSERTION 2 - came from dev. Unchanged from the original; do not drop it.\nif ! git merge-base --is-ancestor \"$head_sha\" \"$dev_sha\"; then\n  echo \"::error::main's new HEAD ($head_sha) is not a commit dev ever held. A merge button or a direct commit produces this, and it is unrecoverable: main stops being an ancestor of dev and can never fast-forward again. See opum-doc docs/runbooks/promote-dev-to-main.md, Rollback.\"\n  exit 1\nfi\n\n# A forced push to main is never needed for a fast-forward, so its presence is\n# the anomaly regardless of what the ancestor tests say. Separate, so the\n# operator is told WHICH thing happened.\nif [ \"$forced\" = \"true\" ]; then\n  echo \"::error::main was FORCE-PUSHED ($before -> $head_sha). A fast-forward promotion never requires --force. Force-push to main needs your own user's direct authority.\"\n  exit 1\nfi\n\n# ASSERTION 1 - moved forward. The half the original never measured.\nif [ \"$before\" = \"$ZERO\" ]; then\n  movement=\"created at $head_sha (main did not exist before this push, so there is no previous position to compare)\"\nelif ! git cat-file -e \"${before}^{commit}\" 2>/dev/null; then\n  echo \"::error::main's previous HEAD ($before) cannot be resolved in this clone, so forward movement CANNOT be proven. Treat as an anomaly, not as a pass - an object that is gone is usually one a rewrite orphaned.\"\n  exit 1\nelif [ \"$before\" = \"$head_sha\" ]; then\n  movement=\"unchanged at $head_sha\"\nelif git merge-base --is-ancestor \"$before\" \"$head_sha\"; then\n  movement=\"moved forward $before -> $head_sha\"\nelse\n  echo \"::error::main was REWOUND or diverged: its previous HEAD ($before) is not an ancestor of its new HEAD ($head_sha). Commits that were on main are no longer on main. This passes an is-ancestor-of-dev check, which is why that check alone was not enough.\"\n  exit 1\nfi\n\n# ASSERTION 3 - main IS dev, not merely part of it. WARNING, NOT A FAILURE, and\n# the distinction is deliberate: dev legitimately advancing between the\n# promotion push and this run produces a non-zero count with nothing wrong, so a\n# hard fail here would cry wolf. It catches ODOC-193 - promoting a STALE LOCAL\n# dev with `git push origin dev:main` instead of `origin/dev:main`, which moves\n# main forward to a commit dev really holds and so satisfies 1 and 2 while\n# delivering less than intended. Raised by opum-fleet, where main is the ref the\n# fleet installs opum-workflow from, so a short promotion ships a stale plugin\n# with everything green.\nbehind=\"$(git rev-list --count \"${head_sha}..${dev_sha}\")\"\nif [ \"$behind\" -gt 0 ]; then\n  echo \"::warning::main is $behind commit(s) BEHIND dev after this push. If dev advanced after the promotion, this is expected and fine. If not, this was a PARTIAL promotion - the classic cause is pushing a stale local dev (git push origin dev:main) instead of the remote-tracking ref (git push origin origin/dev:main), ODOC-193. Left behind:\"\n  git log --oneline --no-decorate \"${head_sha}..${dev_sha}\" | sed 's/^/::warning::  /'\n  completeness=\"but is $behind commit(s) behind dev (see warning)\"\nelse\n  completeness=\"and is dev's tip exactly, with nothing left behind\"\nfi\n\n# States the three things it measured, not \"genuine fast-forward\" generally.\necho \"main $movement; its HEAD is a commit dev holds (dev tip $dev_sha), $completeness. Reported, not enforced - see this script's header.\"","MAINTENANCE TRAP in the v2 script, from lore-web (LWEB-88) after implementing it. The FORCED branch and the REWOUND branch are near-redundant in production: a rewind of main is essentially always forced, so github.event.forced fires first and the REWOUND message becomes near-dead code in CI logs. Both still earn their place - FORCED defaults to false when the script is run by hand, and a rewind performed via the API or a delete-and-recreate need not set it. The trap is a future author noticing the rewind branch never appears in real logs and 'simplifying' it away, which would restore exactly the defect this task exists for. Do not reorder or merge those two branches; if anything, add a test asserting the rewind branch fires with FORCED=false, which our suite already does.","MUTATION-CHECKING THE SUITE, from lore-web, worth copying when OMARK-58 is implemented here. Green tests against a correct script prove nothing about the tests. lore-web reverted its script to the old one-liner and re-ran: 6 of 10 cases went red, and the containment case correctly STAYED green, because the old one-liner preserves that behaviour. A suite that went red on containment too would have been testing the script's identity rather than its properties. That asymmetry is the evidence the suite measures the right thing - a uniformly-red mutation run is a weaker result, not a stronger one. Same technique applies to OMARK-57: mutate check-federated-pins.mjs back to market.plugins.length and confirm ONLY the count/name assertions go red."],"comments":[],"labels":[],"documentation":[],"dependencies":[],"createdAt":"2026-09-15T20:47:01.731Z","updatedAt":"2026-09-15T20:57:44.179Z","blockers":[],"gates":[],"gateEvents":[]}
