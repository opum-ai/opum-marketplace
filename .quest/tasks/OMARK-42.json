{"id":"OMARK-42","aliases":[],"title":"Publish opum-output-styles: repo, plugin, marketplace listing","status":"In Progress","summary":"OPAG-73. Creates and scaffolds a new public repository, opum-ai/opum-output-styles, hosting the fleet's first Claude Code output style ('Show Your Checks', drafted by opum-doc, evidence-led, cleared by the user as written). Location decided after weighing federation's usual drift-avoidance justification (doesn't apply here -- opum-doc authored fresh content, no external CLI to stay in lockstep with) against this fleet's universal one-product-one-repo convention (does apply, no exceptions anywhere else): a new repo won on consistency, not drift-avoidance. Both the user and the orchestrator confirmed creation authority directly and separately from the earlier design/naming approval, matching this session's own insistence on that distinction from OMARK-29-41's saga. Clean from commit one: no operating-block import, no repository profile, no hook-actor identity, .claude/settings.local.json and CLAUDE.local.md gitignored pre-emptively -- verified by grep, not assumed. Scope held to one style, not a set, per the orchestrator's explicit instruction and opum-doc's own restraint.","priority":"High","type":"feature","acceptanceCriteria":[{"index":0,"text":"opum-ai/opum-output-styles exists, public, dev as default branch, main as a fast-forward of dev (bootstrapped by direct push for the first commit, matching the same precedent opum-fleet's own first commit set and recorded honestly, not glossed over).","checked":true},{"index":1,"text":"The style file is byte-identical to opum-doc's own source (docs/reference/opum-output-style-show-your-checks.md's embedded draft on opum-doc's main) -- diffed directly, not retyped from the chat message alone.","checked":true},{"index":2,"text":"Repo scan (grep) confirms zero fleet-internal references: no opum-agent/opum-doc/opum-fleet/OPUM_HOOK/task-id/machine-path strings anywhere in the tracked tree.","checked":true},{"index":3,"text":"marketplace.json lists opum-output-styles as a third federated entry (ref: main, matching opum-workflow's old self-referential pattern -- there is no release-tag concept yet for an unreleased v0.1.0 plugin, so main, not a tag, is the right analog), validated by both the shape checker and the live federated-pin checker locally before landing.","checked":true},{"index":4,"text":"The style is proven to actually LOAD AND CHANGE BEHAVIOR in a real session -- not merely to parse or pass claude plugin validate. Evidence recorded: what was installed, what was activated, what response difference was actually observed. If this cannot be demonstrated, the task says so rather than listing the plugin anyway.","checked":false}],"definitionOfDone":[],"plan":[],"implementationNotes":["AC5 verification result, honest and partial rather than rounded up. Installed for real: claude plugin marketplace update opum, then claude plugin install opum-output-styles@opum. Confirmed installed_plugins.json's gitCommitSha (87b427c) matches main's exact HEAD, not assumed. Debug log (--debug-file) explicitly confirms discovery: 'Loaded 1 output styles from plugin opum-output-styles default directory'. PROVEN CLEANLY: the style's actual prose, when delivered as system-prompt instructions, produces exactly the intended behavioral difference. A/B, same prompt, same session otherwise: baseline response states a verified date and an unchecked Redis claim with identical, undifferentiated confidence; with the style's content injected, the SAME two claims come back explicitly marked -- 'Ran date directly... (verified via command output)' and 'yes, unchecked from general knowledge (not something I verified against this machine or repo)' -- matching the style's own rules precisely, unprompted. NOT CONFIRMED, and I am saying so rather than rounding up: whether the STANDARD activation path -- the outputStyle setting, which is how a real user actually selects a style via /config or settings.local.json -- triggers this same delivery when invoked through claude -p (print/non-interactive mode, which runs through what the debug log calls 'the Agent SDK' entrypoint, cc_entrypoint=sdk-cli). Tried three variants -- settings.local.json file, --settings inline JSON with the frontmatter name 'Show Your Checks', --settings inline JSON with the filename slug 'show-your-checks' -- none produced the style's effect, while direct --append-system-prompt injection worked immediately and consistently every time. The docs describe style-file loading as a terminal-session, startup-time behavior ('Claude Code reads style files when it starts'), which is a different code path than -p/SDK mode -- so this may be a limitation of the non-interactive test method available to me, not a defect in the plugin or the mechanism itself, but I have no non-interactive way to tell the two apart, and I'm not going to claim I ruled it out when I didn't. Leaving this AC unchecked rather than checked-with-caveats: the mechanism that ships the style and the style's own content are both demonstrated working; the end-to-end activation UX through a real /config selection in an interactive session is not something this session could test and is not something to claim was tested."],"comments":[],"labels":[],"documentation":[],"dependencies":[],"references":["opum-agent OPAG-73","opum-doc (Show Your Checks draft)"],"createdAt":"2026-09-10T04:21:01.126Z","updatedAt":"2026-09-10T04:26:29.887Z","blockers":[],"gates":[],"gateEvents":[]}
