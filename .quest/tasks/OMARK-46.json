{"id":"OMARK-46","aliases":[],"title":"Add the Tracker integrity CI job (OPAG-79)","status":"In Progress","description":"Fleet-wide job from OPAG-79: every repository asserts that its tracker reads cleanly and that the managed Quest block is current, so drift and a duplicated task identity are caught by CI rather than by whoever next runs a quest command. Reference YAML authored and negative-tested by opum-web at its origin dev .github/workflows/ci.yml (job tracker). Quest version is DERIVED from CLAUDE.md's own 'Quest CLI <version>' line rather than pinned or floated, so the CI check and the file that declares the contract cannot disagree; a missing line fails loudly instead of silently defaulting.","priority":"High","type":"chore","acceptanceCriteria":[{"index":0,"text":"Tracker integrity job added beside check · manifests, taking its shape from opum-web reference: derive the version from CLAUDE.md and fail loudly if the line is absent, npm install -g @opum-ai/quest at that version, then quest task list --json and quest agents --check --require-installed --target claude as two SEPARATE steps so the failing one names itself.","checked":false},{"index":1,"text":"Adapted honestly where this repo differs rather than copied blind: no .nvmrc or package.json exists here, so setup-node takes a literal node-version and the reason is stated in the file.","checked":false},{"index":2,"text":"Green on dev.","checked":false},{"index":3,"text":"This repo gates main, not dev (ruleset require-ci-on-main, currently requiring only check · manifests). Add Tracker integrity as a second required context there, and only AFTER it has gone green on a real run -- requiring a context that has never reported would block the next dev-to-main fast-forward promotion.","checked":false}],"definitionOfDone":[],"plan":[],"implementationNotes":[],"comments":[],"labels":[],"documentation":[],"dependencies":[],"references":["opum-agent OPAG-79","opum-web ci.yml job tracker (reference implementation)"],"createdAt":"2026-09-11T13:21:19.731Z","updatedAt":"2026-09-11T13:21:36.337Z","blockers":[],"gates":[],"gateEvents":[]}
