{"id":"OMARK-32","aliases":[],"title":"Repoint opum-workflow's marketplace source to explicit ref: main","status":"Done","summary":"Step 4 of 5 in the user-approved main-branch migration -- the step that changes plugin resolution for all nine fleet sessions, announced to the fleet before starting per the standing rule. Changes marketplace.json's opum-workflow plugin entry from the bare string './' (implicit: tracks whatever ref the opum marketplace clone itself is checked out on, which today is dev) to an explicit {source: github, repo: opum-ai/opum-marketplace, ref: main} -- the same mechanism opum-lore and opum-quest already use against other repos with tag refs, proven working in this exact marketplace.json, now self-referential. dev stays the default branch, matching every sibling repo's own model (opum-doc's runbook: 'main | Never the default'); only opum-workflow's own content resolution becomes ref-scoped to main, giving it the same promoted-not-raw-tip property every other plugin here already has. Timed immediately after OMARK-31 deliberately: main and dev are identical right now (both 3d33da9), so this is a no-op in content and a real change in mechanism -- the ideal moment to verify, since nothing is lost if resolution breaks.","priority":"High","type":"chore","acceptanceCriteria":[{"index":0,"text":"marketplace.json's opum-workflow entry uses the explicit github+ref source form, ref: main, landed via the normal dev branch/PR path.","checked":true},{"index":1,"text":"After landing, run claude plugin marketplace update opum in this session and confirm installed_plugins.json's opum-workflow@opum gitCommitSha still resolves correctly -- matching main's tip, not left stale or broken. This is the live-resolution verification opum-agent asked for, isolated to the safest possible moment (main == dev).","checked":false},{"index":2,"text":"Confirm opum-lore and opum-quest's entries are untouched -- they were never expected to be affected (already ref-pinned to tags in other repos), but confirm rather than assume.","checked":true},{"index":3,"text":"If resolution breaks, the rollback is a plain revert of the marketplace.json change back to './' on dev, landed the normal way -- existing clones' dev tracking for the marketplace LISTING itself is never touched by this change, so a bad step 4 means 'opum-workflow does not update', not 'no session can resolve the marketplace'.","checked":true}],"definitionOfDone":[],"plan":[],"implementationNotes":["Verified locally before pushing, against the real checker scripts (not just eyeballing shape): 'manifests OK (3 plugins)' from the exact node snippet ci.yml runs, and check-federated-pins.test.mjs's regression suite still 5/5. Confirmed scripts/check-federated-pins.mjs's live version-match check only fires for refs matching /^v/ (semver tags) -- opum-workflow's new ref: 'main' is a branch, so it is correctly SKIPPED by that check (same as the old './' form and same as any sha pin would be), same treatment opum-lore/opum-quest's real tag pins already get exercised against, live, on any PR touching marketplace.json. opum-lore and opum-quest entries confirmed untouched by this diff (only the opum-workflow object added, three keys changed).","AC2 verification result, partial and stated as partial rather than oversold as complete. Ran `claude plugin marketplace update opum` for real: the marketplace LISTING clone correctly refreshed to dev/main's then-current tip (216d0c4) and marketplace.json's opum-workflow entry parses with the new {source: github, repo: opum-ai/opum-marketplace, ref: main} object -- that half is verified. What could NOT be verified the same way: installed_plugins.json's opum-workflow@opum gitCommitSha stayed at the old e30751d and lastUpdated did not move, because `claude plugin install opum-workflow@opum` reported 'already installed' -- the CLI's own update-dedup logic keys off the declared version string (unchanged at 0.4.0, deliberately, per the release hold), not source identity, so it never attempted a real re-fetch through the new ref path at all. And even if it had: nothing inside opum-workflow's own content (skills/hooks/plugin.json) actually differs between e30751d and main's 216d0c4 -- only marketplace.json and .quest/ changed since they diverged -- so a genuine refetch and a skipped one would look identical in gitCommitSha right now regardless. Chose not to force a cache-clear experiment to manufacture a cleaner signal: that would mean deleting shared local machine state (installed_plugins.json's entry, the cache dir) that other local sessions on this same machine could be relying on mid-session, for a test whose outcome the design does not depend on right now. Net: the fetch-through-ref:main path will get its first real exercise at the next actual version bump -- the eventual stacked release -- not before. That is consistent with the design's intent (nothing should update while the release is held) rather than a defect in it, but it means this AC is satisfied for the listing-resolution half and open for the content-refetch half until that release happens.","Re-promoted immediately after this landed, per opum-agent's note: opened PR #51 dev->main for the CI record (promotion-is-manual failed on purpose as designed, main-is-fast-forward-of-dev correctly skipped for a PR event, all four real CI jobs passed), closed without the merge button, landed with git push origin dev:main. main now at 216d0c4, matching dev exactly -- carries this task's own change rather than sitting one commit behind from the moment the gate existed."],"comments":[],"labels":[],"documentation":[],"dependencies":[],"references":["opum-agent OPAG-69","OMARK-31","opum-lore/opum-quest federated entries (marketplace.json, already-proven pattern)"],"createdAt":"2026-09-09T18:16:54.300Z","updatedAt":"2026-09-09T18:24:50.683Z","finalSummary":"Landed as 216d0c4 (squash of PR #50), then re-promoted to main (PR #51 closed, git push origin dev:main) so main carries it. Listing resolution verified live. Content-refetch-through-new-ref verification is open until the next real version bump, for reasons recorded above -- not a defect, a property of nothing having changed yet.","blockers":[],"gates":[],"gateEvents":[]}
