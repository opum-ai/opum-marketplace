{"id":"OMARK-45","aliases":[],"title":"Clear quest agents --check drift before OPAG-79 puts it in CI","status":"Done","description":"quest agents --check --require-installed --target claude exited 6 here: 'Quest skill file differs from the bundled version.' Diagnosed by reproducing in a scratch copy rather than writing to the repo to find out. Two files differ from what quest 0.6.0 generates: .claude/skills/quest/SKILL.md by one line (AGENTS.md or CLAUDE.md -> AGENTS.md, CLAUDE.md, or GEMINI.md), and CLAUDE.md's managed quest block by its embedded version string only (0.4.0 -> 0.6.0). Only the skill file fails the check: per quest's own contract a version-only block difference reports 'version-only' and exits 0. So the skill file alone clears it, leaving CLAUDE.md untouched -- verified in scratch before applying. lore agents --check is clean for both its SKILL.md and CLAUDE.md.","priority":"Medium","type":"chore","acceptanceCriteria":[{"index":0,"text":"Update .claude/skills/quest/SKILL.md to the bytes quest 0.6.0 generates, and confirm quest agents --check --require-installed --target claude exits 0.","checked":true},{"index":1,"text":"Decide whether to also refresh CLAUDE.md quest block version string 0.4.0 -> 0.6.0. It does not affect the check, but it leaves CLAUDE.md asserting a CLI version that is two minors stale in every session that reads it. Editing CLAUDE.md on a peer-originated prompt is the users call, so it is asked rather than assumed.","checked":true}],"definitionOfDone":[],"plan":[],"implementationNotes":[],"comments":[],"labels":[],"documentation":[],"dependencies":[],"references":["opum-agent OPAG-79"],"createdAt":"2026-09-11T12:32:07.350Z","updatedAt":"2026-09-11T13:14:39.688Z","finalSummary":"quest agents --check --require-installed --target claude now exits 0 with state 'current' (was 6, 'Quest skill file differs from the bundled version'). Diagnosed by reproducing in a scratch git repo rather than writing to this one to find out what would change. The failing difference was .claude/skills/quest/SKILL.md's one-line managed-block target list (gained GEMINI.md), landed in PR #76; CLAUDE.md's block differed only by its embedded version string, which quest's contract explicitly tolerates as 'version-only'. The skill fix alone therefore cleared the check, and the CLAUDE.md refresh 0.4.0 -> 0.6.0 was taken separately to the user rather than folded in, because CLAUDE.md is under their authority and the prompt to touch it came from a peer session. They approved it; applied via quest agents --update-instructions --target claude, which changed exactly one line inside the quest:agent-instructions markers. lore agents --check was clean for both its SKILL.md and CLAUDE.md throughout, so this was quest-only.","blockers":[],"gates":[],"gateEvents":[]}
