---
max_turns: 8
allowed_tools: [Read, Glob, Grep, Skill]
---

Our web app loses whatever the user has typed into a form when they refresh the
tab. How do people usually persist and restore that kind of session state in the
browser?
