schema_version = 1
name = "verification"
description = "Retrieves what counts as proof for a change here — the difference between a shape-valid pin and a content-verified one, and which of the three required CI checks actually measures which claim."
kind = "specialist"
max_tokens = 9000
pinned = ["reference/federated-pin-verification"]
sources = [
  "reference/sdlc-promotion-and-ci-gates",
  "reference/marketplace-federation-contract",
]
