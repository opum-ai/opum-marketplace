{
  "$comment": [
    "Resolved content baselines for the federated entries in .claude-plugin/marketplace.json.",
    "Deliberately NOT stored in marketplace.json: that file is consumed by Claude Code",
    "against its own marketplace schema, and must not carry keys the plugin loader does",
    "not expect. This sidecar is read only by scripts/check-federated-content.mjs.",
    "",
    "Why it exists: a federated entry pins a tag by NAME, and a tag name is mutable.",
    "Re-pointing it at different content is silent - every consumer follows, and the",
    "existing pin check still passes because it only asks whether the tag resolves and",
    "declares the matching version. lore-cli and quest-cli both had v0.6.0 deleted and",
    "recreated on 2026-09-10/11 (OMARK-43); the skill content happened to be unchanged,",
    "but nothing in CI could have told us if it had not been.",
    "",
    "To update: re-resolve the chain yourself (tag ref -> tag object -> commit -> root",
    "tree -> skills subtree), confirm the content is what you intend to ship, and record",
    "the new SHA here in the same change that moves the pin. Updating this file is the",
    "act of saying 'I verified this content', so do not update it to make CI go green.",
    "",
    "Release handshake, agreed with the lore-cli session and tracked there as LCLI-469:",
    "lore-cli messages this repository at tag-cut time with the tag name, the tag object",
    "SHA, the commit it peels to, and the resolved skills/ tree SHA, so a new release",
    "arrives with its baseline rather than as a red check here. The same is expected of",
    "quest-cli. Do not take those SHAs on trust - they are a head start, not a",
    "verification; re-resolve them here before recording, which is the whole point of",
    "this file. If a pin bump ever lands without its baseline moving, the checker fails",
    "on purpose rather than silently checking the wrong tag."
  ],
  "opum-lore": {
    "ref": "v0.9.0",
    "trees": { "skills": "38683e1075994603b7f000517b9e8fc700b7ac21" },
    "verifiedAt": "2026-09-22",
    "note": "OMARK-64. Real content change, not bookkeeping: skills/lore/SKILL.md gained one line documenting the new exit 7 (schema-unattributable). Independently re-resolved via the GitHub API at bump time rather than taken from lore-cli's LCLI-469 handshake messages -- tag ref v0.9.0 -> tag object 48840cb3bc4dd9c6ea16651f8851d56b6a6a6030 (type=tag), also cross-checked via `git ls-remote --tags` as a second independent source -> peels to commit fd712748176b59175eaf40ec10eceb5ba6ac3bdd -> root tree 377de32d2b1e1f98327e6b9dcf9ba8295c3fee67 -> skills subtree 38683e1075994603b7f000517b9e8fc700b7ac21, all matching lore-cli's claimed numbers exactly. .claude-plugin/plugin.json at the tag self-declares version 0.9.0, confirmed by direct GitHub API read. skills/ diff confirmed via the GitHub compare API (v0.8.0...v0.9.0): only skills/lore/SKILL.md changed, +1/-1, nothing else under skills/. npm dist-tags.latest confirmed independently as 0.9.0 on the main package and all 6 platform packages (@opum-ai/lore-{darwin,linux,win32}-{arm64,x64}) -- held at v0.8.0 until publish caught up, same hold-until-published pattern as every prior bump (see this repo's persistent memory, opum-lore-pin-held-at-v0.6.1.md). Release note (tag message) documents that this is the first release stamping committed schemas with a generator digest (LCLI-565, ADR-0005), which makes every schema this repo has committed exit-6 stale until `lore schema export` regenerates them -- expected fallout, not a defect; done in the same change as this pin bump."
  },
  "opum-quest": {
    "ref": "v0.7.1",
    "trees": { "skills": "633c776b3c40ae54ffd1c8cc6fbd7329d03bdd40" },
    "verifiedAt": "2026-09-15",
    "note": "OMARK-56. Bookkeeping bump, zero content change: skills/ at v0.7.1 is byte-identical to the v0.6.0 and v0.7.0 baselines (same subtree SHA 633c776b...). npm dist-tags.latest is 0.7.1, checked independently on the main package and on quest-darwin-arm64, quest-linux-x64 and quest-win32-x64; this repository's own CLAUDE.md managed block moved to 0.7.1 in OMARK-55 the same day. Tag chain resolved independently via the GitHub API: v0.7.1 is a LIGHTWEIGHT tag, so the ref points straight at commit 925801892f40527e1645734d64e687836e3ee0cd with no tag object to peel (v0.7.0 was annotated; check-federated-content.mjs handles both shapes) -> root tree 06c3846db9649b5620f8e11a7aeecbb6e920d069 -> skills subtree 633c776b3c40ae54ffd1c8cc6fbd7329d03bdd40. plugin.json at the tag self-declares version 0.7.1."
  }
}
