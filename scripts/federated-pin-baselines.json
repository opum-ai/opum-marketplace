{
  "$comment": [
    "Resolved content baselines for the federated entries in .claude-plugin/marketplace.json.",
    "Deliberately NOT stored in marketplace.json: that file is consumed by Claude Code",
    "against its own marketplace schema, and must not carry keys the plugin loader does",
    "not expect. This sidecar is read only by scripts/check-federated-content.mjs.",
    "",
    "Why it exists: a federated entry pins a tag by NAME, and a tag name is mutable.",
    "Re-pointing it at different content is silent - every consumer follows, and the",
    "existing pin check still passes because it only asks whether the tag resolves and",
    "declares the matching version. lore-cli and quest-cli both had v0.6.0 deleted and",
    "recreated on 2026-09-10/11 (OMARK-43); the skill content happened to be unchanged,",
    "but nothing in CI could have told us if it had not been.",
    "",
    "To update: re-resolve the chain yourself (tag ref -> tag object -> commit -> root",
    "tree -> skills subtree), confirm the content is what you intend to ship, and record",
    "the new SHA here in the same change that moves the pin. Updating this file is the",
    "act of saying 'I verified this content', so do not update it to make CI go green.",
    "",
    "Release handshake, agreed with the lore-cli session and tracked there as LCLI-469:",
    "lore-cli messages this repository at tag-cut time with the tag name, the tag object",
    "SHA, the commit it peels to, and the resolved skills/ tree SHA, so a new release",
    "arrives with its baseline rather than as a red check here. The same is expected of",
    "quest-cli. Do not take those SHAs on trust - they are a head start, not a",
    "verification; re-resolve them here before recording, which is the whole point of",
    "this file. If a pin bump ever lands without its baseline moving, the checker fails",
    "on purpose rather than silently checking the wrong tag."
  ],
  "opum-lore": {
    "ref": "v0.9.3",
    "trees": { "skills": "ce580cd17d1da6a61ddb53f649398719b9262f96" },
    "verifiedAt": "2026-09-25",
    "note": "OMARK-70. Bookkeeping bump, zero content change: skills/ at v0.9.3 is byte-identical to the v0.9.2 baseline (same subtree SHA ce580cd1...), independently re-resolved via the GitHub API at bump time rather than taken from lore-cli's LCLI-469 handshake messages (sent as two separate messages this time: tag cut, then npm publish confirmation) -- tag ref v0.9.3 -> tag object c07b0ea48a1fd236b436293b20ac34a767c8a8e8 (type=tag), also cross-checked via `git ls-remote --tags` as a second independent source -> peels to commit 819a682c6050dc780ec8919910d4aa5c235a2878 -> root tree e687e7d2d7e27679cf0e5b6f3b3297126cc92f84 -> skills subtree ce580cd17d1da6a61ddb53f649398719b9262f96. .claude-plugin/plugin.json at the tag self-declares version 0.9.3, confirmed by direct GitHub API read. npm dist-tags.latest confirmed independently as 0.9.3 on the main package and all 6 platform packages. Compare API diff (v0.9.2...v0.9.3, 9 commits / 47 files) shows no path under skills/ touched at all -- confirmed by direct tree resolution, not just the absent-diff heuristic. Ran `lore check` (2 files, 0 errors) and `lore agents --check` (both bridge files up-to-date, no regeneration needed) under 0.9.3 via `npx --yes @opum-ai/lore@0.9.3`, not the older global install."
  },
  "opum-quest": {
    "ref": "v0.10.0",
    "trees": { "skills": "633c776b3c40ae54ffd1c8cc6fbd7329d03bdd40" },
    "verifiedAt": "2026-09-25",
    "note": "OMARK-69, dispatched by opum-agent (OPAG-378) alongside the lore 0.9.2 pin (OMARK-66), following opum-cli-e2e's TASK-106 qualification of quest 0.10.0 + lore 0.9.2 together. Bookkeeping bump spanning 100 commits and 172 changed files (v0.7.1...v0.10.0 compare API), but zero content change under skills/: the compare API diff lists no path under skills/ as touched, and independent tree resolution confirms the skills subtree SHA is byte-identical to the v0.7.1 baseline (633c776b...) -- a git tree SHA hashes its content, so this is proof, not an absence-of-diff heuristic. Tag chain resolved independently via the GitHub API and cross-checked via git ls-remote --tags: v0.10.0 is an ANNOTATED tag (unlike v0.7.1's lightweight one) -> tag object 90a8d0ea08a48656a76a39ce52eddce279e9b68f -> peels to commit bf30c5cff28c61b050712d7193e1f9f8bece7718 -> root tree 282565ecb387f20545da1648372522cc6d1cbbaa -> skills subtree 633c776b3c40ae54ffd1c8cc6fbd7329d03bdd40. plugin.json at the tag self-declares version 0.10.0, confirmed by direct GitHub API read. npm dist-tags.latest confirmed independently as 0.10.0 on the main package and all 6 platform packages. This repository's own CLAUDE.md managed Quest block moved to 0.10.0 the same day (OMARK-68)."
  },
  "proof-skills": {
    "ref": "v0.1.1",
    "trees": { "skills": "24b1cbc1685248b1a3b2a30ffb2b854a43efd050" },
    "verifiedAt": "2026-09-25",
    "note": "OMARK-73, dispatched by opum-agent (OPAG-437, operator ruling 2026-09-25: MIT-license and publish proof-skills under Opum AI). First pin of a fourth, non-federated-by-history plugin -- proof-skills is a new public repo, not a spin-off of an existing pin. Layout question (a git-subdir source into plugins/proof-skills/ vs. repo-root) was measured against this repository's own check-federated-pins.mjs, check-federated-content.mjs, and generate-codex-marketplace.mjs: none handle git-subdir without a code change (classifyEntry hardcodes source==='github'; resolveSubtree only walks one level into the root tree; generate-codex-marketplace.mjs's own test already locks in 'throw on git-subdir'), and Codex's per-plugin source has no documented subdirectory field. opum-agent ruled repo-root. v0.1.0 (the plugins/proof-skills/ subdir layout) shipped and stays published, unpinned here, and is never re-cut -- re-pointing it would be the exact tag-mutation shape OMARK-43 exists to catch. v0.1.1 ships the layout moved to the repo root instead, as a new tag. Tag chain resolved independently via the GitHub API and cross-checked via git ls-remote --tags (not taken from opum-agent's relay): v0.1.1 is an ANNOTATED tag -> tag object eb96f9681363f34d369612059814e17a300afd8f -> peels to commit 578749fd8baa6456d89d7a0037c88bc09a89213d, which is also the repository's main HEAD (confirmed via git/ref/heads/main) -> root tree 9a53258d828aee4b8d474b6428f8dc0fbf9661d5, carrying .claude-plugin/ and skills/ directly at the root (no plugins/ subdirectory) -> skills subtree 24b1cbc1685248b1a3b2a30ffb2b854a43efd050, containing formal-verify, lean-model, proof-simplify, and tlaplus-model. .claude-plugin/plugin.json at the tag self-declares name proof-skills, version 0.1.1, author {name: Opum AI, url: https://github.com/opum-ai}, license MIT -- confirmed by direct GitHub API contents read, matching what opum-agent relayed. Repository confirmed public (private: false) via the repos API, and MIT confirmed independently via the /license endpoint (spdx_id MIT)."
  }
}
